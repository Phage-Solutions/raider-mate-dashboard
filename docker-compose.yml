# One service, because the deployed unit is one container: Caddy and the node process
# both come up inside it. See Dockerfile.
services:
  dashboard:
    build: .
    env_file: .env
    ports:
      - '8081:8080'
    restart: unless-stopped
